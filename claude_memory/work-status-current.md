# 지금 상태 — 두 창이 같이 보는 한 장

> **갱신 260822.** 세션 시작할 때 `git pull` 하고 이것부터 읽는다.
> 여기가 "지금 뭐 하는 중인지"의 정본이다. 보드(Drive)는 전체 그림, 이 파일은 오늘.

## 0. 두 창 분업 — 이거 안 지키면 서로 덮어쓴다

이 리포에는 이미 무인배포 경로(GAS → main 직접 쓰기)가 있다. 여기에 창이 둘 붙으면 셋이 된다.

| 창 | 하는 일 | 절대 안 하는 일 |
|---|---|---|
| **로컬(clasp 있는 PC)** | GAS 프로젝트에 코드 붙이기 · `-i` 재배포 · 콘솔 설정 · 실측 | **리포에 커밋 금지** |
| **클라우드(claude.ai/code)** | 리포 코드·문서·정본 작성 → 브랜치+PR | GAS 배포·콘솔 조작(못 함) |

**규칙 3개**
1. **리포 커밋은 클라우드만.** 로컬은 `git pull`로 받아만 간다. 로컬에서 고치고 싶은 게 생기면 고치지 말고 이 파일 §4에 한 줄 적어 클라우드에 넘긴다.
2. **한 파일을 두 창이 동시에 안 건드린다.** 지금 누가 뭘 잡고 있는지는 §3에 적는다.
3. 푸시 전 `git fetch origin main` — 무인배포가 방금 밀었을 수 있다.

## 1. 지금 붙는 중 (로컬 몫) — 순서대로, 하나 끝나고 다음

전부 `gas/BATTLE_배선_인계.md`에 순서·검증기준까지 다 써 있다. **그 문서가 작업지시서다.**

- [ ] **0순위** 응대 절단·되묻기 — 기존 브릿지 호출부의 `max_tokens` 확인(1200 이하면 그게 원인)
- [ ] **0.5순위** 뇌 1호 재배포(실명 표기 반영)
- [ ] **1순위** 발송 전 관문 — 중복 차단 + 언어 게이트
- [ ] **1.5순위** 환자 기억 — 슬랙 스레드 이력 + 메모 + **차트번호 줍기**
- [ ] **1.6순위** 원장 실측 피드백(#차트번호 매칭 확장 등)
- [ ] **2순위** 라인 ↔ OK차트
- [ ] **3순위** `^` 이어받기 (슬랙 Events URL 등록 필요)
- [ ] **4순위** 캐싱

## 2. 원장 손이 필요한 것

- [ ] **LINE Webhook redelivery OFF** — developers.line.biz → 채널 → `Messaging API` 탭 → Webhook settings.
      ⚠ **`Use webhook`은 절대 끄지 말 것.** redelivery만 끈다. 중복 응답의 절반이 여기서 끝난다.
- [ ] **일본어 골든셋 2~3건** — 잘 나온 응대 캡처. 톤 어색함은 예시 없이는 못 고친다.
- [ ] 아크로인입봇에 슬랙 `channels:history` 읽기 권한 (기억 로더가 스레드를 읽으려면 필요)

## 3. 지금 누가 뭘 잡고 있나

| 대상 | 잡은 창 | 상태 |
|---|---|---|
| `gas/*.gs`, `gas/BATTLE_*.md` | 클라우드 | PR #40 열림 |
| 라인브릿지 GAS 프로젝트 | 로컬 | 배선 중 |
| 카카오 상담톡 | **미정 — 아래 §5** | 착수 전 |

## 4. 로컬 → 클라우드 인계함 (로컬이 여기 적으면 클라우드가 고쳐서 PR)

- (비어 있음)

## 5. 다음 큰 것 — 카카오 상담톡

레이니를 끊었으므로 판단은 우리 몫이다. **아직 붙이지 말고 확인만.**

확인할 4가지:
1. 웹훅 받는 주소로 **GAS `/exec` 주소를 쓸 수 있나** (고정 IP·도메인 인증서를 요구하는지)
2. **응답 시간 제한**이 몇 초인가, 넘기면 재전송하는가 (라인은 2초 → 중복 응답 사고가 났다)
3. 인증 방식 (토큰? 서명 검증?)
4. Cloudflare 같은 중계가 정말 필요한가 — 상담톡은 레이니로 이미 붙였던 전례가 있다

**권장 분업**: 확인·설계·코드는 클라우드, 카카오 비즈니스 채널 설정과 실제 붙이기는 로컬.

## 6. 오늘 끝난 것 (260822)

- 이어받기 토글 `#` → **`^`** (`#`은 해시태그 메모와 겹쳐 배제)
- 응대 절단 처방 — 출력 상한 2000, **끊긴 답은 발송 금지**(사람 인계)
- 되묻기 처방 — `accAsk_`에 기억 인자 추가 + **슬랙 스레드에서 지난 대화 실제 조회**
- **차트번호 줍기** — 직원이 스레드에 적은 "차트 20193 이름"을 환자맵에 기록.
  실측: 환자맵 J환자 140행 중 차트번호 있는 건 34행(24%)뿐이고 전부 표시이름 자동매칭이었다.
  사람이 알아낸 번호는 지금까지 한 건도 시스템에 안 들어왔다.
- 메모를 한국어로 강제 (일본어 대화의 메모가 일본어로 쌓여 직원이 못 읽었다)
- UrlFetch 예산 정리 — 이력 조회 90초 캐시, 차트 줍기는 **외부 호출 0회**
- 프로젝트보드 v2.9 (Drive `01_운영중`)
